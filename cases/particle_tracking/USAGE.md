# Usage — Cyclone Particle Tracking

This planned case will use `DPMFoam / lagrangian`.

Expected workflow:

```bash
cd cases/particle_tracking
# generate mesh
# run solver
# post-process validation metrics
```

Generated mesh and solver outputs should stay out of Git.
